#include "lists.h"

/**
 * get_nodeint_at_index - This function gets
 * a node at a certain index.
 *
 * @head: The head node.
 *
 * @index: The index to fetch from.
 *
 * Return: The node.
 */
listint_t *get_nodeint_at_index(listint_t *head, unsigned int index)
{
	if (head == NULL)
	{
		return (NULL):
	}
	while (head != NULL)
	{
		if (head ==
	}
}
