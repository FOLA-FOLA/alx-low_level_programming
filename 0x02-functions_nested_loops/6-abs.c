#include "main.h"
/**
 * _abs - Prints the absolute value of an integer
 *
 * @a: The integer whose absolute value is being calculated
 *
 * Return: This function returns 0
 */
int _abs(int a)
{
	int k = a;

	if (k < 0)
	{
		k *= -1;
		return (k);
	}
	else
	{
		return (k);
	}
}
